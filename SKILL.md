---
name: textbook-editor
description: |
  Refine and normalize textbook-style LaTeX documents.
  Preserve all knowledge content while improving style consistency,
  readability, and LaTeX formatting.
  Input: tex_source. Output: refined.tex.
---

# 《人工智能通识教程》（第二版）——书稿写作风格通用指南

> 技能适配：适配 textbook-editor 技能

> 输入变量：tex_source

> 输出变量：refined_tex

> 使用说明：本文件是整本书的写作规范与 AI 辅助创作指令，适用于所有章节的新增、修改和扩写任务。写作时，可将本文件与待修改的章节 .tex 文件一并提供给 AI，并在末尾追加具体任务说明。

---

## 一、全书定位与基本基调

**书名：** 《人工智能通识教程》（第二版）

**核心定位：** 面向高校非计算机专业学生及普通读者的人工智能通识教材。读者无需具备数学或编程基础，但需要在读完本书后，对 AI 的原理、应用和社会影响形成理性、立体的认知。

**基本基调：**

- **可读性第一**：技术内容必须用生活化语言表达，优先用类比、故事和场景，而非定义和公式。
- **严谨不失温度**：医学、科研等专业章节保持学术准确，但行文不能枯燥。
- **有边界感**：所有章节都需清楚区分"AI能做什么"与"AI不能做什么"，避免夸大或贬低。
- **启发而非灌输**：鼓励读者带着问题阅读，带着思考离开，而不是被动接受结论。

---

## 二、全书章节结构总览

```
微模块1（第1—4章）  AI通识与基础认知
  第1章  认知智能演进与时代变革
  第2章  AI认识世界：从学习到创造
  第3章  从提示词到智能体：AI从"对话"走向"主动执行"
  第4章  AI的伦理、风险与社会影响

微模块2（第5章）    人工智能赋能科研
  第5章  AI赋能科研

微模块3（第6章）    人工智能实践操作
  第6章  AI工具实践

微模块4（第7—10章）人工智能在医工方向的应用
  第9章  人工智能赋能医学的应用基础知识
  第10章 人工智能在医工融合中的应用
```

---

## 三、写作风格分类规范

全书因受众和内容性质不同，分为两种主要写作风格，须严格按章节类型套用：

---

### 风格 A：叙事科普型（适用于第1—5章）

**核心特征：** 以故事驱动内容，以人物带动知识，以类比解释概念，以场景引发思考。

#### A1 开篇方式

每章开篇不使用"本章将介绍……"式的官方体，而是用一个**具体的历史时刻、生活场景或人物故事**切入：

```
✅ 正确示范（chapter_1.tex 开篇）：
1946年，世界上第一台通用电子计算机ENIAC在美国宾夕法尼亚大学诞生。
这台重达30吨的庞然大物……近八十年后的今天，你口袋里的那部智能手机，
其运算能力是ENIAC的数百万倍。

✅ 正确示范（chapter_3.tex 开篇）：
2015年，一款名叫Slack的办公协作软件开始在美国科技公司中流行……
十年之后，大二学生小林在期末考试前夜打开了手机上的AI学习助手……

❌ 错误示范：
本章将系统介绍人工智能的历史发展脉络及其在现代社会的应用现状。
```

#### A2 人物塑造

可引入虚构学生角色（小杨、小林等）作为读者代入视角，但须保持以下原则：

- 人物行为和场景须真实可信，贴近大学生日常
- 同一人物在同一章节内前后一致，不混用
- 人物只用于引出场景，不作为论证依据

#### A3 段落与节奏

- 段落长度：120—250 字之间，保持呼吸感，避免超过 300 字的巨型段落
- 每节末尾或关键转折前，使用三星分隔符制造节奏停顿：

```latex
\begin{center}
\textcolor{gray!70}{*\ \ *\ \ *}
\end{center}
\vspace{12pt}
```

- 关键概念第一次出现时，用 `\textbf{}` 加粗，之后正常行文

#### A4 本章导览写法

每章正文在开篇叙事段落后，须设置一个"问题导览"区块，其形式为列举本章要回答的若干个核心问题。

导览格式统一如下（参考 chapter_1.tex 实际写法）：

```latex
[开篇叙事段落……]

在进入正文之前，请带着以下 [数字] 个问题来阅读本章：

\begin{enumerate}[leftmargin=*, itemsep=2pt]
\item \textbf{问题一？} 一句话阐释或提示方向。
\item \textbf{问题二？} 一句话阐释或提示方向。
……
\end{enumerate}

[引导句，过渡到第一节]
```

导览的三要素：
1. **大致内容**：开篇叙事段落（≈150—300字）用历史时刻／生活场景／人物故事引入本章主题，交代背景语境和讨论范围
2. **章节结构**：问题列表中的每个问题隐含着本章某节的核心内容；问题数量 = 关键知识点数（风格A第1—5章建议6—9题，风格B第6—10章建议4—6题）
3. **本章本质问题**：每个问题必须以一句话点出该知识点的本质追问（如"智能是否只属于人类？"），不能是浅层记忆性问题（如"什么是智能？"）

导览与接下来的正文之间，必须使用三星分隔符隔开：

```latex
\begin{center}
\textcolor{gray!70}{*\ \ *\ \ *}
\end{center}
\vspace{12pt}
```

#### A5 图示格式（风格 A 专用）

```latex
\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{./images_chapter_XX/X.X.png}
\caption*{\footnotesize\itshape\color{gray!50} 图X-X\quad 图片说明文字}
\end{figure}
```

- 图题使用斜体灰色（`\itshape\color{gray!50}`），与正文图题风格区分
- 浮动参数用 `[H]`（强制就地放置，适合叙事流中穿插图示）
- 占位图格式（尚未生成图片时）：

```latex
\begin{figure}[H]
\centering
\begin{tcolorbox}[width=0.88\textwidth, colback=gray!10, colframe=gray!50, boxrule=0.5pt, arc=2pt]
\centering
\vspace{35pt}
{\large\color{gray!70} [插图占位]}
\vspace{15pt}
{\small\color{gray!50} 图X-X：图片内容说明（建议风格描述）}
\vspace{25pt}
\end{tcolorbox}
\caption*{\footnotesize\itshape\color{gray!50} 图X-X\quad 图片标题}
\end{figure}
```

#### A6 脚注使用

引用外部数据、产品特性或研究结论时，用脚注标注来源：

```latex
\footnote{参见Netflix Tech Blog中关于个性化封面推荐算法与A/B测试的技术细节。}
```

---

### 风格 B：学术教材型（适用于第6—10章，医学/工程应用模块）

**核心特征：** 结构严谨，层次清晰，术语准确，每一小节有引导句、正文和小结，逻辑闭合。

#### B1 开篇方式

每章或每节开头，用 **1—2 句简洁的临床/工程场景句**切入，点明现实问题，再过渡到本节内容：

```
✅ 正确示范（chapter_7.tex 开篇）：
清晨的医院大厅刚刚亮起叫号屏，挂号、问诊、抽血、拍片、用药和随访
已经开始不断产生信息。过去，这些信息大多停留在纸张、胶片和医生经验
之中；如今，它们正在进入电子病历、影像系统、监护设备和健康管理平台……

✅ 正确示范（chapter_8.tex 小节）：
夜班病房里，医生刚提交一组新医嘱。屏幕上只是几行文字，实际却牵连着
药物选择、检查安排、护理要求、监测项目和执行时间……

❌ 错误示范：
本节介绍医嘱智能判别与规范评分系统的基本框架和技术原理。
```

#### B2 标题层级规范

```latex
% 一级：章
\chapter{章节标题}

% 二级：节（自动编号 9.1、9.2……）
\section{节标题}

% 三级：小节（自动编号 9.1.1……）
\subsection{小节标题}

% 四级：子小节（无编号，但有序号前缀，粗体）
\subsubsection*{\textbf{1. 子节标题}}
\subsubsection*{\textbf{2. 子节标题}}
……
\subsubsection*{\textbf{N. 小结}}   % 每个 subsection 末尾必有小结
```

**注意：** 第四级标题统一无编号（`*`），但内容中用数字前缀（1. 2. 3.）手动排序，粗体。

#### B3 图文引用方式

每个小节的**引言段落**必须包含图引用句，且图必须紧跟在引言段落后：

```
如图\ref{fig:ch9_XX}所示，……[用一句话概括图的核心信息]。
```

图示格式（风格 B 专用）：

```latex
\begin{figure}[htbp]
\centering
\begin{tcolorbox}[width=0.88\textwidth, colback=gray!10, colframe=gray!50,
  boxrule=0.5pt, arc=2pt, boxsep=2pt, left=2pt, right=2pt, top=2pt, bottom=2pt]
\centering
\includegraphics[width=\linewidth]{./images_chapter_07-08/X.X.png}
\end{tcolorbox}
\caption{\footnotesize\bfseries 图片标题（不含"图X.X"前缀，LaTeX 自动编号）}
  \label{fig:ch9_XX}
\end{figure}
```

- 浮动参数用 `[htbp]`（允许 LaTeX 自动优化位置）
- 图题使用粗体黑色（`\bfseries`）
- 必须设置 `\label`，与正文 `\ref` 配对

#### B4 列表格式

风格 B 中的列表统一使用以下格式，条目结构为"**名称：** 解释文字"：

```latex
\begin{itemize}
  \item \textbf{名称}或名称加冒号：解释内容，一般 2—4 句，说清含义、特点和用途。

  \item \textbf{名称二}：……（条目之间空一行，保持视觉间隔）
\end{itemize}
```

#### B5 小结写法

每个 `\subsection` 末尾必须有一个无编号小结（格式固定）：

```latex
\subsubsection*{\textbf{N. 小结}}

[1个段落，约80—120字]
用一句话概括本节讲了哪几个核心内容，
再用一句话说明它们之间的关系或在整体知识框架中的位置，
最后用一句话点出与下一节的衔接关系（可选）。
```

#### B6 版面控制（风格 B 章节开头必须添加）

```latex
\raggedbottom
\setlength{\textfloatsep}{10pt plus 2pt minus 2pt}
\setlength{\floatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{10pt plus 2pt minus 2pt}
```

---

## 四、通用写作原则（两种风格均适用）

### 4.1 句子与段落

| 规则 | 说明 |
|------|------|
| 句子长度 | 优先使用短句（25字以内）；长句不超过50字，且必须有清晰的主语 |
| 段落结构 | 每段有明确的中心句，开头或结尾点明本段核心观点 |
| 衔接词 | 使用"因此""然而""随着""与此同时""不过"等，而非"首先/其次/再次/最后"堆叠 |
| 冗余表达 | 避免"在……方面""对于……而言""需要指出的是""值得注意的是"等惯用空话 |
| 被动语态 | 尽量少用，优先用主动句式；数据和事实陈述除外 |

### 4.2 术语与概念

- 专业术语**第一次出现**时，给出简洁的中文解释或类比，不得直接引入而不解释
- 英文缩写第一次出现时给出全称：`PET（正电子发射断层扫描）`
- 不造新词，不滥用"赋能""颠覆""重塑"等空洞形容词
- 数字表达：小于100的数字用汉字（三个、十次），100以上用阿拉伯数字（512层、40万例）

### 4.3 禁止事项

- ❌ 不写"本节将……"/"本章主要包括……"式的无信息量开头
- ❌ 不写"综上所述，……"/"总而言之，……"式的机械结尾
- ❌ 不使用"非常""十分""极为""相当"等程度副词堆叠
- ❌ 不写没有具体内容支撑的大判断（如"AI彻底改变了……"而不给出具体说明）
- ❌ 不在同一段落中重复使用相同的词语或句式
- ❌ 不添加与当前写作任务无关的注释、说明、总结性文档

### 4.4 数据与案例引用

- 引用具体数据时，必须有来源语境（"某研究团队"/"公开报道显示"/"监管文件要求"）
- 案例以**功能说明**为目的，不做宣传，不过度美化任何具体产品
- 教材中的数字以"数量级准确"为标准，避免将快速变化的精确数字写死

### 4.5 内容长度控制

各章节写作时须严格控制内容篇幅，避免过度展开或冗长赘述。

| 章节类型 | 每章总字数 | 每节字数 | 每小节字数 | 段落字数 |
|----------|-----------|---------|-----------|---------|
| 风格A（第1—5章） | 6,000—10,000字 | 1,500—3,000字 | 500—1,500字 | 120—250字 |
| 风格B（第6—10章） | 4,000—6,000字 | 1,000—2,000字 | 300—800字 | 100—200字 |

**字数约束规则：**

- **每章总字数**：风格A不超过10,000字，风格B不超过6,000字。超过时须删减次要案例或合并冗余段落
- **每小节字数**：风格A小节（subsection）最低500字、最高1,500字。低于500字表示内容单薄，高于1,500字说明该小节包含过多内容，应拆分为两个子小节
- **段落字数**：严格执行120—250字区间。超过300字的段落必须拆分，低于80字的段落应合并或充实
- **章节字数**：章内各节之间字数差异不应超过1倍——如某节2,000字而另一节仅500字，须平衡分配内容
- **图中文字量**：每张插图中的文字标注不超过3行（约20字），说明文字不超过50字，避免图内冗长文本

**精简原则：**
- 优先使用类比和场景来压缩技术解释，而不是逐步骤展开
- 案例分析控制在1—2个段落，避免变成产品功能介绍
- 课后习题每道不超过35字，总题数8—12题
- 本章小结不超过500字，延伸阅读每篇附注不超过30字

---

## 五、课后习题写作规范

每章末尾设"课后习题X"，题目类型以**简答题和分析题**为主，数量视章节体量为8—12题。

**写法要求：**

- 题目语言简洁，直接指向核心知识点
- 不出纯记忆性填空或选择题
- 每道题不超过35字
- 最后1—2题应有一定开放性，引导读者结合实际思考

**格式：**

```latex
\section*{课后习题X}

\begin{enumerate}
\item 题目文字。

\item 题目文字。
……
\end{enumerate}
```

---

## 六、插图规范

### 6.1 图号命名规则

| 模块 | 章节 | 图号格式 | 路径 |
|------|------|---------|------|
| 微模块1（第1—4章） | 第1章 | 图1-1、图1-2 | `./images_chapter_01-03/` |
| 微模块1（第1—4章） | 第2章 | 图2-1、图2-2 | `./images_chapter_01-03/` |
| 微模块1（第1—4章） | 第3章 | 图3-1、图3-2 | `./images_chapter_01-03/` |
| 微模块2（第5章） | 第5章 | 图5-1、图5-2 | `./images_chapter_05/` |
| 微模块4（第9章） | 第9章 | 图9.1、图9.2 | `./images_chapter_07-08/` |
| 微模块4（第10章） | 第10章 | 图10.1、图10.2 | `./images_chapter_07-08/` |

### 6.2 AI 生图统一风格规范

所有新增插图，在生成时须在提示词中加入以下**通用风格描述**：

> 高品质医学/科技教材级信息图，扁平化矢量插画风格，等距视角与正视图结合，画面整洁专业；主色调采用科技蓝（深蓝 #0D3B66、宝蓝 #1E88E5、天空蓝 #4FC3F7、薄荷青 #26A69A）配以白色（#FFFFFF）和浅灰背景（#F5F8FC），关键节点使用珊瑚橙（#FF7043）或暖红（#E53935）作为强调色；线条干净利落，圆角矩形卡片、细描边图标、柔和阴影；字体为简体中文无衬线字体（类思源黑体/苹方），字号清晰可读，标题加粗，正文常规；构图均衡居中，留白充足，避免拥挤；不要使用照片真实风格，不要出现卡通儿童画风，不要带杂乱手绘笔触，不要出现乱码，所有文字必须为正确的简体中文。

**通用否定提示词：**
> 真人照片，摄影写实，3D渲染过度，卡通儿童画，漫画线条，杂乱笔触，模糊，低分辨率，错别字，乱码，英文乱码，水印，logo

**画幅规范：**
- 横版信息图：**16:10**（2400×1500 px，或更高）
- 圆环/闭环图：**4:3**

### 6.3 插图字体与色彩详细规范

| 规范项 | 规格要求 |
|--------|----------|
| **背景色** | 插图背景统一使用纯白色 `#FFFFFF`，不采用灰色底（如 `colback=gray!10`） |
| **中文字体** | 简体中文无衬线字体：**思源黑体（Source Han Sans SC）** 或 **苹方（PingFang SC）**；不允许使用宋体、楷体等衬线字体 |
| **西文字体** | 无衬线西文字体：**Helvetica** 或 **Arial**；斜体使用 **Helvetica Oblique**；不允许使用 Times New Roman 衬线字体作为图内西文 |
| **最大字号** | 图内标题／主标签：**18pt**（对应 LaTeX 的 `\Large`），仅用于图表标题区和核心标注 |
| **最小字号** | 图内说明文字／注释：**10pt**（对应 LaTeX 的 `\small`），不得小于此值以确保印刷可读 |
| **正文图注字号** | 图注／表格注脚：**12pt**（对应 LaTeX 的 `\normalsize`），与正文统一 |
| **标题颜色** | 图内标题 / 关键标签：**科技蓝 #1E88E5**，加粗 |
| **正文颜色** | 图内说明文字：**深灰 #333333**，常规字重 |
| **强调色** | 关键节点／数据高亮：**珊瑚橙 #FF7043** |
| **背景装饰色** | 浅灰底色区块：**#F5F8FC**（仅用于区分层次，主背景仍为白色） |
| **输出分辨率** | 所有插图导出为 **300 DPI**，确保印刷清晰度 |

---

## 七、表格规范

全书所有表格须严格遵从以下统一格式标准，确保跨章节视觉一致性。

### 7.1 表格通用格式

```latex
\begin{table}[htbp]
\centering
\small
\begin{tabular}{|c|c|c|}
\hline
\textbf{表头1} & \textbf{表头2} & \textbf{表头3} \\ \hline
内容A & 内容B & 内容C \\ \hline
内容D & 内容E & 内容F \\ \hline
\end{tabular}
\caption{\footnotesize\bfseries 表格标题（不含"表X-X"前缀，LaTeX 自动编号）}
\label{tab:chX_XX}
\end{table}
```

### 7.2 表格格式规范明细

| 规范项 | 规格要求 |
|--------|----------|
| **表格位置** | 使用 `[htbp]` 浮动参数，允许 LaTeX 自动优化位置 |
| **字体大小** | 表内文字使用 `\small`（约10pt），表头加粗 `\textbf{}` |
| **边框** | 必须设置垂直和水平边框（`|c|c|` 格式），不能使用无边框样式 |
| **表头** | 第一行为表头行，加粗居中，上下均有横线分隔 |
| **表题** | 采用 `\caption{}` 自动编号，粗体 `\bfseries`，字号 `\footnotesize`（约9pt） |
| **标签** | 必须设置 `\label{}`，格式 `tab:chX_XX`（如 `tab:ch1_01`），与正文 `\ref{}` 配对 |
| **表正文引用** | 在表格之前段落中必须引用，格式："如表\ref{tab:ch1_01}所示，……" |
| **对齐方式** | 文字列左对齐（`l`），数字列居中（`c`），数值列右对齐（`r`） |
| **列宽** | 避免过宽列，每列宽度不超过页面宽度的1/3；超出时可考虑横排转竖排 |
| **表注** | 当表格需要额外注释时，在 `\end{tabular}` 之后、`\caption{}` 之前添加 `\multicolumn` 行 |
| **跨页** | 短表（≤10行）使用 `table` 浮动环境；长表使用 `longtable` 环境并重复表头 |

### 7.3 表格类型规范

| 表格类型 | 用途 | 行数限制 | 示例 |
|----------|------|---------|------|
| **规范对照表** | 对比不同概念、方法或维度的异同 | ≤8行 | 风格对比表、维度对比表 |
| **数据统计表** | 呈现关键数据、指标或成果 | ≤12行 | 实验数据表、调查统计表 |
| **术语速查表** | 整理本章核心概念和术语 | ≤15行 | 核心概念名词表 |
| **流程步骤表** | 列出操作步骤或流程节点 | ≤10行 | 实现步骤表、评估流程表 |

### 7.4 表格中的文字规范

- 表头文字不超过 6 个字，使用名词性短语（如"模型名称""应用场景"）
- 表内单元格文字不超过 20 字，多于 20 字的内容应分入多行或改用列表表达
- 数值须对齐小数点或单位（如"85.3%""1,024 px"），同一列内单位必须统一
- 禁止在表格内使用脚注、列表或段落格式——复杂说明应移出表格放在表注中

---

## 八、LaTeX 格式快速参考

### 常用命令速查

```latex
% 加粗强调
\textbf{关键词}

% 斜体
\textit{术语}

% 脚注
\footnote{来源说明。}

% 图引用
如图\ref{fig:ch9_01}所示

% 无编号节（加入目录）
\section*{标题}
\addcontentsline{toc}{section}{标题}

% 无编号章节小结
\section*{本章小结}

% 三星分隔符（风格 A）
\begin{center}
\textcolor{gray!70}{*\ \ *\ \ *}
\end{center}
\vspace{12pt}

% 版面控制（风格 B 章节开头）
\raggedbottom
\setlength{\textfloatsep}{10pt plus 2pt minus 2pt}
\setlength{\floatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{10pt plus 2pt minus 2pt}
```

### 特殊环境（按章节类型）

**风格 A 专用 tcolorbox（第1—4章）：**
```latex
% 名词解释框（蓝色）
\begin{knowledgebox}
内容
\end{knowledgebox}

% 趣味故事框（橙色）
\begin{funbox}
内容
\end{funbox}

% 实用技巧框（绿色）
\begin{tipbox}
内容
\end{tipbox}
```

**风格 B 图示框（第7—10章）：**
```latex
\begin{tcolorbox}[width=0.88\textwidth, colback=gray!10, colframe=gray!50,
  boxrule=0.5pt, arc=2pt, boxsep=2pt, left=2pt, right=2pt, top=2pt, bottom=2pt]
\centering
\includegraphics[width=\linewidth]{路径}
\end{tcolorbox}
```

**通用 skillbox（两种风格均可用）：**
```latex
\begin{tcolorbox}[skillbox, title={本节知识要点}, before skip=4pt, after skip=4pt]\small
读完本节，你将能够：
\begin{itemize}[nosep]
\item 理解知识转化中的三个规律（信息压缩律、话语转换律、传播伦理律），知道在传播中信息可以压缩但性质、方向、确定性不能改
\item 区分三种传播形态的信息组织逻辑：海报是浏览式（靠视觉层级）、PPT是听觉线性（靠三段重复）、科普是注意力竞争（结论先行）
\item 用禁忌词清单和"更准确地说"段落在科普转化中守住科学底线——相关不能变因果，可能性不能变确定性
\item 理解基金申请书与论文的写作逻辑差异：论文写"已经做了什么"，基金写"将要做什么以及为什么做得出来"
\end{itemize}
\end{tcolorbox}
```

> **使用说明：** `skillbox` 用于每节开头，以"本节知识要点"标题列出学习目标，帮助读者在阅读前建立预期。条目标题简洁即知识点关键词，列表后不追加冗余。适用于全书所有章节（风格A和风格B均可）。

---

## 九、AI 辅助写作指令模板

在向 AI 提交写作任务时，将本文件与目标 .tex 文件一起提供，并在末尾追加以下格式的任务说明：

```
【写作任务说明】

目标文件：chapter_X.tex
目标章节：第X章 第X.X节 《节标题》
任务类型：[新增小节 / 扩写已有内容 / 修改段落 / 补充习题]
写作风格：[风格 A（叙事科普型）/ 风格 B（学术教材型）]
字数目标：约 XXXX 字（对应约 X 页）
新增图：[是（图号9.XX / 图X-X）/ 否]

具体要求：
[在此说明具体写什么内容、需要涵盖哪些知识点、
 与上下文的衔接关系，以及其他特殊要求]
```

---
